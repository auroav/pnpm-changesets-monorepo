{
  "name": "pnpm-changesets-monorepo",
  "version": "0.0.0",
  "description": "A monorepo example with pnpm and changesets.",
  "scripts": {
    "watch": "pnpm --parallel --filter=./packages/* run watch",
    "build": "pnpm -r --filter=./packages/* run build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "rimraf": "^3.0.2"
  }
}
